import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * The Tutorial class displays the tutorial text messages in the first world map.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Tutorial extends UI
{
    //----- Object image -----
    public GreenfootImage window = new GreenfootImage("ui/window.png");
    
    /**
     * Tutorial Constructor: Sets the tutorial first tutorial text image.
     */
    public Tutorial()
    {
        setImage(window);
    }
    
    /**
     * Act - do whatever the Tutorial wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }    
}
